# SILT Core v0.2 — Release Candidate Checklist

**Status:** External review, final machine-readable validation and citation/archive metadata preparation complete; ready for publication mechanics

This checklist records the remaining publication work between SILT Core v0.2 Freeze Candidate 1 and the public `v0.2.0` release.

It does not amend SILT Core semantics.

## Completed before external review

- Canonical semantic architecture at `spec/v0.2/semantic-architecture.md`.
- WE01–WE04 pre-freeze worked encounters completed.
- Machine-readable conformance fixtures and schema packaged as non-normative companion material.
- Experimental SILT ↔ LCP mapping packaged as non-normative companion material.
- v0.1 → v0.2 repository reconciliation completed.
- README, overview, positioning, roadmap, threat model, governance, contribution and security documentation reconciled to v0.2 terminology.
- `CHANGELOG.md` added with `v0.2.0 — Unreleased` entry.
- Draft GitHub `v0.2.0` release notes completed.
- Licensing boundary documented in `LICENSING.md`.
- Apache 2.0 and CC BY 4.0 release licence notices packaged under `LICENSES/`.
- AUT CISRC / Vietsch material preserved on its v0.1 semantic basis and marked as historical / transitional where needed.
- FC1 editorial consistency corrections completed for deliberate non-expression, semantic non-inference and named adjacent technologies in normative Core.

## External reviewer gate — COMPLETE

External review was completed before merge to `main`, before creation of the `v0.2.0` tag, and before Zenodo / DOI publication.

The overall reviewer disposition was:

> **READY SUBJECT TO EDITORIAL CORRECTIONS**

No BLOCKER findings were identified.  
No MAJOR findings were identified.  
No FC2 is required.

The three review items were disposed as follows:

- LCP mapping basis line: already corrected and confirmed as FC1 / RC4.1.
- §3 Participant list: `DAO` removed to avoid an unnecessary technical-organisational asymmetry.
- §10 / §11 boundary: §11 now cross-references the general Technical Capability ≠ Authority rule in §10.

The detailed disposition is recorded in:

[`docs/release/v0.2-review-disposition.md`](./v0.2-review-disposition.md)

The reviewer also found no release-blocking contradiction between the canonical Core and the reviewed WE01–WE04 pressure-test evidence, no material release overclaim, and no architectural ontology capture requiring Core reopening.

## Final machine-readable validation — COMPLETE

A reproducible GitHub Actions validation workflow now lives at:

`.github/workflows/v0.2-conformance.yml`

It validates the FC1 conformance fixture schema, WE01–WE04, manifest inventory and internal reference consistency, and confirms that the experimental LCP machine-readable mapping identifies FC1 as its SILT basis.

GitHub Actions run `35302485302` completed successfully on commit:

`e6de2a7dd1566f8653604ab5df606f420bdf4ee9`

The `validate` job and `Validate FC1 fixtures and references` step both completed with conclusion `success`.

The detailed validation record is:

[`docs/release/v0.2-final-validation.md`](./v0.2-final-validation.md)

Final machine-readable release-candidate validation result:

> **PASS**

No semantic reopening is required.

## Citation and archive metadata — PREPARED

Repository citation metadata is now recorded in:

[`CITATION.cff`](../../CITATION.cff)

The release title, creator, abstract, keywords, repository and project URL are prepared for `v0.2.0`. DOI and publication-date fields are intentionally not invented before publication.

The Zenodo/archive metadata plan is recorded in:

[`docs/release/v0.2-zenodo-metadata.md`](./v0.2-zenodo-metadata.md)

That record preserves the functional licensing split: **CC BY 4.0** for the canonical specification and human-readable documentation; **Apache License 2.0** for reference code and implementation-oriented machine-readable artefacts. The Zenodo record must not misrepresent the whole archive as governed by one licence where the repository contains both classes of material.

The remaining archive fields are publication-derived only: release date, version DOI, concept DOI where applicable, tagged release commit SHA and final GitHub release URL.

## Reviewer bundle

The completed reviewer bundle comprised:

- `spec/v0.2/semantic-architecture.md`;
- `CHANGELOG.md`;
- `docs/migration/v0.1-to-v0.2-repository-audit.md`;
- `tests/conformance/v0.2/README.md`;
- `tests/conformance/v0.2/manifest.json`;
- WE01–WE04 worked encounters;
- draft `v0.2.0` release notes;
- `LICENSING.md`;
- this checklist;
- `docs/release/v0.2-external-review-brief.md`.

## Remaining before tag

- Confirm all release links resolve from `main` after merge.
- Update the public website to v0.2 before or atomically with the release announcement.
- Merge the release branch to `main`.
- Replace `Unreleased` in `CHANGELOG.md` with the release date.
- Add final publication-derived fields to `CITATION.cff` and the Zenodo record once assigned.
- Create the `v0.2.0` tag.
- Publish GitHub release and Zenodo / DOI record.
- Record release checksum / commit SHA where appropriate.

Repository description and homepage metadata have been updated to the v0.2 positioning and `https://siltcore.org/` respectively.

## Freeze discipline after external review

The semantic review gate is closed.

No further semantic editing should occur before `v0.2.0` unless final mechanical validation reveals an actual contradiction, missing artefact or implementation-independent semantic defect.

Editorial, metadata and publication corrections may proceed without reopening Core. Any later semantic change should be recorded transparently as an erratum or proposed for a subsequent version rather than introduced silently.

## Website release requirement

The public website must not continue to present v0.1 concepts such as universal Capacity, Status or “authority semantics for digital action” as the current SILT Core architecture once v0.2 is released.

The v0.2 website should foreground:

> **Source → Standing → Presentation → evaluation at the encounter**

and describe SILT as a semantic architecture for mutual legibility across plural legal, customary, cultural, private, institutional and technical orders without requiring ontological collapse.

The website update should remain public-facing and concise. It should not reproduce the full normative Core or collapse the wider project into an identity / authorisation product.
