# SILT Core v0.2 — Final Machine-Readable Validation

**Status:** PASS  
**Branch:** `release/v0.2-fc1`  
**Validated commit:** `e6de2a7dd1566f8653604ab5df606f420bdf4ee9`  
**Workflow:** `SILT Core v0.2 conformance`  
**Run:** `35302485302`

This report records the final machine-readable release-candidate validation for SILT Core v0.2.

It does not amend SILT Core semantics.

## Validation performed

A reproducible GitHub Actions workflow was added at:

`.github/workflows/v0.2-conformance.yml`

The workflow validates the FC1 conformance material under `tests/conformance/v0.2/` by:

- loading the v0.2 conformance fixture schema as JSON Schema Draft 2020-12;
- checking that the schema itself is valid;
- validating WE01–WE04 against that schema;
- checking the fixture inventory against `manifest.json`;
- checking participant, Source, Standing, Evidence, Presentation, Authority, Action, Attribution, Obligation, Profile Expression and condition references for unresolved internal references where the fixture model defines them;
- checking that evaluation results use only `SATISFIED`, `NOT_SATISFIED` or `INDETERMINATE`;
- checking that deliberate non-expression remains outside the evaluation-result vocabulary;
- checking that fixture `architecture_reference` values identify FC1;
- checking that the manifest records PASS for both JSON Schema validation and reference consistency for WE01–WE04;
- parsing the experimental LCP machine-readable mapping and confirming that it identifies FC1 as its SILT basis.

The workflow treats the LCP mapping as a separate experimental artefact class rather than a conformance fixture, consistent with the conformance README and manifest.

## Result

GitHub Actions run `35302485302` completed successfully on commit:

`e6de2a7dd1566f8653604ab5df606f420bdf4ee9`

The `validate` job completed with conclusion:

> **success**

The validation step `Validate FC1 fixtures and references` completed successfully.

No machine-readable release blocker was identified.

## Scope of this result

This PASS establishes release-candidate integrity for the packaged conformance fixtures and their internal reference discipline.

It does **not** convert the fixture schema into a normative SILT wire format, object model or serialisation. The machine-readable conformance material remains test notation only.

It also does not establish that SILT Core is complete for all possible future domains. That question remains outside the v0.2 release gate.

## Release disposition

Final machine-readable conformance validation:

> **PASS**

No semantic reopening is required.

The remaining pre-release work is publication and release mechanics: final link checks after merge, repository metadata, website cut-over, citation/archive metadata, CHANGELOG dating, merge, tag, GitHub release and Zenodo/DOI publication.
