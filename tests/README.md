# Tests

The primary SILT Core v0.2 semantic tests are the unified conformance fixtures in `conformance/`.

Earlier v0.1 misuse-case tests are preserved under `archive/v0.1/tests/` and do not define v0.2 semantics.
